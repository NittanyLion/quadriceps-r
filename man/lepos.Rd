% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{lepos}
\alias{lepos}
\title{Positive-weight cubature rule for the uniform weight on the cube}
\usage{
lepos(d, q = NULL, p = NULL, normalize = TRUE, pragmatic = FALSE)
}
\arguments{
\item{d}{Dimension, \code{d >= 1}.}

\item{q}{Number of nodes of the one-dimensional Gauss rule whose exactness is wanted,
\code{q >= 1}. Give \code{q} or \code{p}, not both.}

\item{p}{Degree of exactness, \code{p >= 0}, as an alternative to \code{q}.}

\item{normalize}{With \code{TRUE} (the default), the weight is the uniform density on \verb{[0,1]^d}:
the weights sum to 1 and the rule computes \verb{E f(U)} for \code{U} uniform on the unit cube.
With \code{FALSE}, the rule is for the plain integral over \verb{[-1,1]^d}, the convention of
\code{statmod::gauss.quad(kind = "legendre")}, and the weights sum to \code{2^d}.}

\item{pragmatic}{As for \code{\link[=ghpos]{ghpos()}}: \code{FALSE} signals an error of class \code{quadriceps_no_rule}
when no stored rule covers the request; \code{TRUE} returns the cheapest tensor product of
lower-dimensional rules (stored rules and one-dimensional Gauss-Legendre rules).}
}
\value{
A list with components \code{nodes} (\verb{n x d} matrix, one node per row) and \code{weights}
(\code{n} strictly positive weights).
}
\description{
The smallest positive-weight rule the package has for the uniform weight on a
\code{d}-dimensional cube.
}
\details{
\code{lepos(d, q)} follows one-dimensional Gauss-Legendre functions such as
\code{statmod::gauss.quad(q, kind = "legendre")}: \code{q} is the number of nodes of the
one-dimensional Gauss rule, and \code{lepos(d, q)} returns a rule with the same exactness in \code{d}
dimensions, degree \verb{p = 2q - 1}, that replaces the \code{q^d}-node product grid; for \code{d = 1} it
is the \code{q}-node Gauss-Legendre rule itself. Alternatively give the degree \code{p}.

The details given for \code{\link[=ghpos]{ghpos()}} apply here too. All nodes of every stored Le rule lie
inside the cube.
}
\examples{
r <- lepos(2, 5)                                   # degree 9: 17 nodes on [0,1]^2 instead of 25
sum(r$weights * r$nodes[, 1]^3 * r$nodes[, 2]^2)   # 1/4 * 1/3

r <- lepos(2, 5, normalize = FALSE)                # the same rule on [-1,1]^2, weights sum to 4
r <- lepos(6, 6, pragmatic = TRUE)                 # d = 6 from a product of stored rules
}
\seealso{
\code{\link[=ghpos]{ghpos()}}, \code{\link[=nnodes]{nnodes()}}, \code{\link[=ruleinfo]{ruleinfo()}}, \code{\link[=available_rules]{available_rules()}}
}
