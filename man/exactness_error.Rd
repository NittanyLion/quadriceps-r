% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{exactness_error}
\alias{exactness_error}
\title{Largest relative monomial error of a rule}
\usage{
exactness_error(nodes, weights = NULL, p, family)
}
\arguments{
\item{nodes}{An \verb{n x d} matrix of nodes, one per row (or a rule: a list with components
\code{nodes} and \code{weights}, in which case \code{weights} is taken from it).}

\item{weights}{The \code{n} weights.}

\item{p}{Degree up to which exactness is checked.}

\item{family}{\code{"gh"} or \code{"le"}.}
}
\value{
The error, a single number.
}
\description{
The maximum, over all monomials \code{x^a} of total degree \verb{<= p}, of
\verb{|sum_i w_i x_i^a - E x^a| / max(sum_i |w_i| |x_i^a|, 1)}, for the normalized weight of
\code{family} (\code{"gh"}: \code{N(0, I_d)}; \code{"le"}: uniform on \verb{[0,1]^d}). The denominator is the scale
of the sum being computed, so a value near machine epsilon means the rule is exact to
rounding. Rules returned with \code{normalize = FALSE} must be checked in the normalized frame.
}
\examples{
r <- ghpos(4, 5)                       # degree 2 * 5 - 1 = 9
exactness_error(r, p = 9, family = "gh")
}
