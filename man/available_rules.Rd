% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{available_rules}
\alias{available_rules}
\title{List the stored rules}
\usage{
available_rules(family)
}
\arguments{
\item{family}{\code{"gh"} (Gaussian weight) or \code{"le"} (uniform weight on the cube).}
}
\value{
A data frame with columns
\itemize{
\item \code{family}, \code{d} (dimension), \code{q}, \code{p} (degree of exactness, \verb{p = 2q - 1}), \code{n} (number of
nodes);
\item \code{moller}: Möller's lower bound on \code{n}, or \code{-1} where it is not tabulated; a rule with
\code{n == moller} is proven minimal;
\item \code{relerr}: largest relative monomial error of the stored double-precision rule, measured
when the data were built (see \code{\link[=exactness_error]{exactness_error()}});
\item \code{minweight}: smallest weight (always \verb{> 0});
\item \code{interior}: \code{TRUE} when every node lies inside the integration domain;
\item \code{origin}: who the rule belongs to: \code{"own"}, or \code{"derived: ..."}, \code{"same-rule: ..."},
\code{"transcribed: ..."} followed by the published source;
\item \code{file}: file name under \verb{extdata/<family>/}.
}
}
\description{
All stored rules of a family, one row per rule, sorted by dimension and degree. Tensor
products, which \code{pragmatic = TRUE} builds on demand, are not listed.
}
\examples{
head(available_rules("gh")[, c("d", "q", "p", "n")])
}
