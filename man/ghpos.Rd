% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{ghpos}
\alias{ghpos}
\title{Positive-weight cubature rule for the Gaussian weight}
\usage{
ghpos(d, q = NULL, p = NULL, normalize = TRUE, pragmatic = FALSE)
}
\arguments{
\item{d}{Dimension, \code{d >= 1}.}

\item{q}{Number of nodes of the one-dimensional Gauss rule whose exactness is wanted,
\code{q >= 1}. Give \code{q} or \code{p}, not both.}

\item{p}{Degree of exactness, \code{p >= 0}, as an alternative to \code{q}.}

\item{normalize}{With \code{TRUE} (the default), the weight is the standard normal density
\verb{(2 pi)^(-d/2) exp(-|x|^2 / 2)}: the weights sum to 1 and the rule computes \verb{E f(Z)} for
\code{Z ~ N(0, I_d)}. With \code{FALSE}, the weight is \verb{exp(-|x|^2)}, the convention of
\code{statmod::gauss.quad(kind = "hermite")}, and the weights sum to \code{pi^(d/2)}.}

\item{pragmatic}{What to do when no stored rule covers the request. With \code{FALSE} (the
default), signal an error of class \code{quadriceps_no_rule}. With \code{TRUE}, return the cheapest
(fewest nodes) tensor product of lower-dimensional rules instead: stored rules and
one-dimensional Gauss-Hermite rules, combined over the split of \code{d} that minimizes the
number of nodes. Such a product is a valid positive-weight rule of the requested degree;
it is just not small. With \code{TRUE} the product is also returned in the rare case that it
has strictly fewer nodes than the stored rule, so the result is always the cheapest the
package can build.}
}
\value{
A list with components \code{nodes}, an \verb{n x d} matrix with one node per row (also for
\code{d = 1}), and \code{weights}, a vector of \code{n} strictly positive weights.
}
\description{
The smallest positive-weight rule the package has for the Gaussian weight in \code{d}
dimensions.
}
\details{
\code{ghpos(d, q)} follows one-dimensional Gauss-Hermite functions such as
\code{statmod::gauss.quad(q, kind = "hermite")}: there, \code{q} is the number of nodes of the
one-dimensional Gauss rule, which is exact to degree \verb{2q - 1}. Here, \code{ghpos(d, q)} returns
a rule with the same exactness in \code{d} dimensions, degree \verb{p = 2q - 1}, that replaces the
\code{q^d}-node product grid; for \code{d = 1} it is the \code{q}-node Gauss-Hermite rule itself.
Alternatively give the degree: \code{ghpos(d, q)} is \code{ghpos(d, p = 2 * q - 1)}.

With \code{rule <- ghpos(d, q)}, \code{sum(rule$weights * f(rule$nodes))} (for a vectorized \code{f} of the rows)
equals the integral of \code{f} against the weight for every polynomial \code{f} of total degree
\verb{<= p}, up to rounding; \code{\link[=ruleinfo]{ruleinfo()}} gives the measured error of each stored rule.

Rules are stored for \verb{2 <= d <= 5} at odd degrees, up to a ceiling that depends on \code{d}
(see \code{\link[=available_rules]{available_rules()}}). A request is served by the smallest stored rule of degree
\verb{>= p}, so an even \code{p} gets the rule for \code{p + 1}. For \code{d = 1} the Gauss rule is computed on
the fly (Golub-Welsch) and is never an error.
}
\examples{
r <- ghpos(3, 4)                                   # degree 7: 27 nodes instead of 4^3 = 64
sum(r$weights * r$nodes[, 1]^2 * r$nodes[, 2]^4)   # E[Z1^2 Z2^4] = 3
identical(ghpos(3, p = 7), r)

try(ghpos(3, 21))                                  # nothing stored at that degree
r <- ghpos(3, 21, pragmatic = TRUE)                # tensor product of lower-dimensional rules
r <- ghpos(7, 5, pragmatic = TRUE)                 # d = 7 as (d = 2) x (d = 5)
}
\seealso{
\code{\link[=lepos]{lepos()}}, \code{\link[=nnodes]{nnodes()}}, \code{\link[=ruleinfo]{ruleinfo()}}, \code{\link[=available_rules]{available_rules()}}
}
