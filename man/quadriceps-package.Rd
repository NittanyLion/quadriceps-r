% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadriceps-package.R
\docType{package}
\name{quadriceps-package}
\alias{quadriceps}
\alias{quadriceps-package}
\title{quadriceps: positive-weight cubature rules for the Gaussian weight and the cube}
\description{
Positive-weight cubature rules in \code{d >= 1} dimensions for two weight functions:
}
\details{
\itemize{
\item \strong{GH} (\code{\link[=ghpos]{ghpos()}}): the Gaussian weight, by default the standard normal density
\code{N(0, I_d)};
\item \strong{Le} (\code{\link[=lepos]{lepos()}}): the uniform weight, by default the uniform density on \verb{[0,1]^d}.
}

A rule of degree \code{p} integrates every polynomial of total degree \verb{<= p} exactly. All
weights are strictly positive. The rules shipped with the package are the smallest ones
known to its author; \code{\link[=available_rules]{available_rules()}} lists them, and \code{LICENSE.note} and the files \code{RULES.md} and
\code{NOTICE.md} of the repository say where each one comes from.

The second argument \code{q} of \code{\link[=ghpos]{ghpos()}} and \code{\link[=lepos]{lepos()}} is the number of nodes of the
one-dimensional Gauss rule whose exactness is wanted, as in
\code{statmod::gauss.quad(q, kind = "hermite")}; the rule returned has degree \verb{p = 2q - 1}.
The degree can be given instead, as the argument \code{p}.

This package is the R twin of the Julia package Quadriceps.jl and of the Python package
quadriceps; the three share their data and their conventions.
}
\examples{
r <- ghpos(3, 4)        # as exact as the 4x4x4 Gauss-Hermite grid (degree 7), with 27 nodes
dim(r$nodes)
r <- ghpos(3, p = 7)    # the same rule, requested by degree
r <- lepos(2, 5)        # degree 9 on the unit square: 17 nodes instead of 25
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/NittanyLion/quadriceps-r}
  \item Report bugs at \url{https://github.com/NittanyLion/quadriceps-r/issues}
}

}
\author{
\strong{Maintainer}: Joris Pinkse \email{pinkse@gmail.com} [copyright holder]

Authors:
\itemize{
  \item Joris Pinkse \email{pinkse@gmail.com} [copyright holder]
}

Other contributors:
\itemize{
  \item Moustapha Diallo (some of the stored rules for the cube descend from his published rules (MIT license, see LICENSE.note)) [copyright holder]
}

}
\keyword{internal}
